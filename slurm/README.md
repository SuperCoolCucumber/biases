# Slurm Templates

This directory keeps Slurm guidance and portable templates only.

- `templates/generic_bias_job.slurm`: generic single-job template.
- `generated/`: local rendered scripts; ignored by Git.

Before using any Slurm script on a new infrastructure, update scheduler
directives, artifact paths, GPU/memory settings, and secrets handling.
